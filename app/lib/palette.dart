import 'package:flutter/material.dart';

const background = Colors.black;
const primary = Colors.white;

const jordan_background = LinearGradient(
  colors: [
    Color(0xFF027FFC),
    Color(0xFF1E3B89),
  ],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);
const jordan_yellow = Color(0xFFFFE200);
const jordan_blue = Color(0xFF164FAB);

const high_red = Color(0xFFA4041E);
const high_background = LinearGradient(
  colors: [
    high_red,
    high_red
  ]
);

const air_background = LinearGradient(
  colors: [
    Color(0xFF00DAD2),
    Color(0xFF1A10FF),
  ],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);
const air_orange = Color(0xFFFF560B);
const air_blue = Color(0xFF124DF1);
const air_light_blue = Color(0xFF25D9F0);
